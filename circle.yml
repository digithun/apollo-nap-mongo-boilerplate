machine:
  node:
    version: 8.9.0
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

compile:
  override:
    - yarn build
test:
  override:
    - yarn test

deployment:
  production:
    branch: master
    commands:
      - DEPLOY_TARGET=root@188.166.251.184
      - ./.bin/deliver.bash
      - ssh -t $DEPLOY_TARGET 'cd /opt/jamplay-comment-service&&ls'
      # sh ./bin/up.sh